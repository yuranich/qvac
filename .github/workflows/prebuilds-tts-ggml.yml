# Build-only workflow.  Publishing is handled by on-merge-tts-ggml.yml
name: "Prebuilds (TTS GGML)"

on:
  workflow_dispatch:
    inputs:
      workdir:
        description: "Working directory for the package"
        type: string
        required: false
        default: "packages/tts-ggml"
  workflow_call:
    inputs:
      ref:
        description: "ref"
        type: string
      repository:
        type: string
        required: false
        default: "tetherto/qvac"
      workdir:
        description: "Working directory for the package"
        type: string
        required: false
        default: "packages/tts-ggml"

permissions:
  contents: read

jobs:
  prebuild:
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    uses: tetherto/qvac/.github/workflows/reusable-prebuilds.yml@tmp-self-hosted-runners
    with:
      workdir: ${{ inputs.workdir }}
      ref: ${{ inputs.ref }}
      repository: ${{ inputs.repository }}
      artifact-name-prefix: tts-ggml-
      linux-extra-packages: pkg-config libx11-dev libxcb1-dev libxkbcommon-dev
      setup-python-on-windows: true
      include-vulkan-sdk: true
    secrets: inherit
